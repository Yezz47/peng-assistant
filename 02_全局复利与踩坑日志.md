# 02｜全局复利与踩坑日志

## 复利经验

### 2026-09-05｜本地预览与公网部署边界
- 经验：`localhost` 只代表当前电脑上的本地服务，服务停止、电脑关机或换设备后无法访问。
- 适用场景：Web/H5原型验收、二维码与NFC测试。
- 推荐做法：本地开发使用 `localhost`；对外演示使用Vercel等平台提供的HTTPS地址。
- 参考路径/命令：`pnpm dev --port 3000`

### 2026-09-05｜生成式功能必须具备降级链路
- 经验：作品集演示不能因模型密钥缺失或接口超时完全中断。
- 适用场景：AI原型、面试演示、低预算MVP。
- 推荐做法：真实AI与受约束本地模板共享输入校验和事实白名单；接口异常自动降级并标识生成方式。
- 参考路径/命令：`src/lib/ai/reviewGenerator.ts`

## 踩坑记录

### 2026-09-05｜预览页面无法打开
- 现象：浏览器访问 `localhost:3000` 被拒绝。
- 原因：开发服务已停止，3000端口没有程序监听。
- 解决方法：在项目目录重新启动Next.js开发服务。
- 验证结果：商家页面返回HTTP 200。
- 避免再次发生：对外展示使用公网地址；本地验收前先确认服务状态。

### 2026-09-05｜同步目录时依赖文件被占用
- 现象：复制整个项目时无法访问 `next-swc.win32-x64-msvc.node`。
- 原因：运行中的Next.js进程占用原生依赖文件。
- 解决方法：只同步 `src`、配置和迁移文件，不复制 `node_modules` 与 `.next`。
- 验证结果：源码成功同步，11项测试通过。
- 避免再次发生：依赖在目标项目单独安装；禁止把构建缓存和依赖目录当作业务源码同步。

### 2026-09-05｜受限目录触发依赖自检写入失败
- 现象：包管理器自检尝试创建临时文件并出现 `EPERM`。
- 原因：目标目录写权限受限；具体自动自检触发条件不作为通用结论。
- 解决方法：依赖完整时直接调用项目本地测试、Lint和TypeScript程序；需要写入时申请目录权限。
- 验证结果：测试、源码Lint和关闭增量写入的类型检查通过。
- 避免再次发生：先区分代码错误与环境权限错误，不把工具链写入失败误判为测试失败。

### 2026-09-08｜Vercel CLI登录配置写入失败
- 现象：CLI可以下载并输出版本，但登录时保存配置出现 `EXDEV: cross-device link not permitted`。
- 原因：当前受限运行环境对用户配置目录的临时文件重命名存在跨目录限制；更细触发条件待验证。
- 解决方法：本项目采用GitHub推送后在Vercel网页导入的路径，不依赖本机CLI登录。
- 验证结果：CLI版本59.11.7可运行；网页导入结果待GitHub远端建立后验证。
- 避免再次发生：作品集部署优先选择Git仓库集成；不要把CLI配置错误误判为Next.js构建失败。

## 待验证
- 不同手机浏览器的语音识别可用率与剪贴板权限差异。
- Vercel免费环境中的冷启动体验和大众点评移动端跳转行为。
