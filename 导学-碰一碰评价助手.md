# 导学｜碰一碰评价表达助手

## 1. 前置知识（面试高频标注）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
| --- | --- | --- | --- |
| AI产品边界与人在回路 | 解释为何只生成、不代替用户提交 | 草稿编辑、复制、平台跳转 | 高 |
| Prompt约束与事实落地 | 防止模型虚构评价事实 | 事实白名单、生成提示词 | 高 |
| 结构化输入与状态建模 | 把模糊体验转成稳定输入 | 星级、标签、菜品、原文 | 高 |
| 降级与容错 | 确保无密钥或接口失败仍可演示 | AI调用失败回退本地模板 | 高 |
| 隐私与数据最小化 | 说明评价原文如何处理 | 敏感信息检测、可选留存 | 高 |
| 产品验证指标 | 区分可运行与被用户接受 | 完成率、用时、复制率、符合度 | 高 |

## 2. 重点亮点与学习顺序（先看这个）

| 亮点标题 | 为什么重要 | 通用技术关键词 | 先看哪些文件 | 顺序 |
| --- | --- | --- | --- | --- |
| 配置驱动的多商家适配 | 一套产品加载不同门店上下文 | 配置化、动态路由、数据模型 | `src/data/merchants.ts`、`src/app/m/[merchantId]/page.tsx` | 1 |
| 可信生成约束 | 评价不能脱离用户事实 | 事实白名单、Prompt约束、输出校验 | `src/lib/validation/feedback.ts`、`src/lib/ai/reviewGenerator.ts` | 2 |
| 分层降级 | 模型异常不应阻断演示 | 超时、异常隔离、本地fallback | `src/lib/ai/reviewGenerator.ts` | 3 |
| 人在回路的交互闭环 | 最终表达权与提交权属于用户 | 可编辑草稿、显式复制、外部跳转 | `src/components/FeedbackFlow.tsx` | 4 |
| 可验证质量门禁 | 产品主张需要证据 | 单元测试、类型检查、回归场景 | `src/lib/**/*.test.ts` | 5 |

## 3. 必备知识点

- [ ] 能画出“入口—商家确认—输入—校验—生成—编辑—复制—平台”的主流程。
- [ ] 能解释结构化标签为什么不能直接等价于“好评诱导”。
- [ ] 能说明AI事实白名单、生成后校验和本地降级分别解决什么风险。
- [ ] 能区分代码测试、可用性测试、市场验证和商业结果。
- [ ] 能解释为什么作品集阶段不接账号系统、不做自动提交、不优先接数据库。

## 4. 推荐阅读（结合仓库）

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
| --- | --- | --- | --- | --- |
| 商家域模型 | 类型与配置边界 | `src/types/domain.ts`、`src/data/merchants.ts` | 20分钟 | 新增商家需要改什么、不需要改什么 |
| 页面状态 | 前端状态与交互约束 | `src/components/FeedbackFlow.tsx` | 35分钟 | 为什么复制前不能跳平台 |
| 输入验证 | Schema与业务规则分层 | `src/lib/validation/feedback.ts` | 30分钟 | 如何处理星级与倾向冲突 |
| AI生成 | Prompt、fallback、事实列表 | `src/lib/ai/reviewGenerator.ts` | 35分钟 | AI失败后为什么仍能输出 |
| 输出质检 | 生成后安全检查 | `src/lib/validation/generatedDraft.ts` | 25分钟 | 如何发现虚构菜品与诱导表达 |
| 证据体系 | 测试与产品指标 | `src/lib/**/*.test.ts`、`04_用户测试记录模板.md` | 25分钟 | 目前哪些结论已证实、哪些待测 |

## 5. 自学提醒

若某文件或原理看不懂，请继续追问 AI；本技能负责给学习路径与题目，不提供逐行讲解。

## 6. 项目技术定位

AI产品与前后端交叉原型：核心价值不是训练模型，而是把商家上下文、用户真实输入、生成约束、降级策略与人机协作流程组合成可验证产品。

## 7. 核心原理解析

1. **上下文适配：** 每商家单独写应用会产生维护分叉；项目把变化收敛为商家档案，通过动态路由加载，底层交互与生成逻辑复用。
2. **事实落地：** 自由生成可能虚构菜品和体验；项目把标签、菜品和原文转成允许事实，再约束生成和检查声明使用的事实。
3. **倾向一致：** 星级、标签和文字可能互相冲突；项目在生成前检查明显冲突，让用户修正而不是由模型猜测。
4. **分层降级：** 外部模型可能未配置、超时或报错；项目捕获异常并回退本地模板，使演示链路可持续。
5. **人在回路：** 自动发布会越过用户控制和平台边界；项目只提供可编辑草稿，复制与跳转都需要用户主动操作。

## 8. 关键设计决策

| 决策 | 备选 | 取舍 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| 方向B增强版 | 直接唤起并自动填写平台 | 多一步复制，但避免账号和自动提交风险 | 用户可能流失 | 测复制率与完成时长 |
| 配置化商家档案 | 每商家独立版本 | 复用底层，新增商家只加配置 | 档案需要人工维护 | 双商家互不串数据测试 |
| 真实AI+本地fallback | 只用真实AI或只用模板 | 兼顾能力展示和演示稳定性 | fallback表达多样性有限 | 断网/无密钥场景测试 |
| 作品集暂不接数据库 | 首轮即建设完整数据平台 | 降低部署复杂度 | 暂无自动漏斗数据 | 先用5—8人测试表记录 |

## 9. 量化与验证（含待测）

- 已验证：11项自动测试；双商家配置；自定义菜品进入草稿；输入与输出安全规则。
- 待测：陌生用户完成率、中位完成时长、复制率、内容符合度。
- 建议样本：首轮5—8人，用于发现问题，不用于市场规模推断。
- 建议成功标准：无严重阻塞；多数用户可在60秒左右完成；具体结果以真实记录为准。
