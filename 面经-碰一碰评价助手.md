# 面经｜碰一碰评价表达助手

## 1. 项目简介（简历可用）

面向餐饮堂食场景设计并落地配置化评价表达H5原型，将星级、体验标签、菜品和用户原文转化为受事实约束的可编辑草稿；通过生成前校验、生成后质检、失败降级和人在回路，兼顾表达效率与真实性边界。

## 2. 简历 bullet

- **配置驱动的场景扩展：** 针对每家商户独立开发会造成代码分叉的问题，将商家名称、地址、品类、菜品与平台入口抽象为档案，由动态入口加载同一交互和生成链路；已用两份商家配置验证底层复用，公开授权与规模化维护仍待验证。
- **可信生成约束：** 针对评价生成可能虚构体验的问题，把星级、标签、预设及自定义菜品和原文归一为允许事实，约束模型引用并对生成稿进行二次校验，使当前自动测试可识别未选择菜品、诱导评价和倾向冲突。
- **分层容错降级：** 针对演示环境缺少密钥、外部模型超时或报错会阻断主流程的问题，设计OpenAI-compatible调用与本地忠实模板的降级链路，在不改变输入校验和事实边界的情况下保证原型可交互；真实接口效果待配置密钥后验证。
- **人在回路与隐私边界：** 针对自动代写、自动发布和原文留存风险，将产品限定为可编辑草稿工具，复制与平台跳转均由用户主动触发，同时加入敏感信息提示与脱敏处理；不读取评价平台账号，不自动填写或提交。
- **证据驱动的产品验证：** 将“代码可运行”与“用户愿意使用”拆分，已用11项测试验证关键规则，并定义完成率、中位用时、复制率和内容符合度的小样本测试口径；真实用户指标尚未产生，不在简历中提前承诺。

## 3. 面试问题

### 主题A｜产品判断与范围

#### Q1：为什么选择餐饮评价表达，而不是更通用的AI写作？

**第一人称口播：** 我最初关注的不是“再做一个写作工具”，而是一个具体的行为断点：消费者已经完成用餐，也有真实感受，但在组织语言和切换评价平台时容易放弃。我的任务是验证这个断点是否值得用AI降低门槛，因此把场景限定在餐饮堂食，把输入收敛为星级、体验维度、菜品和一句话。这样既能利用商家上下文提高相关性，又能把虚构风险限制在可检查范围。当前结果是完成了可交互原型和规则验证，但真实完成率仍需小样本测试，不能把产品假设写成市场结论。

**追问1：为什么首轮不做外卖？口播：** 我把外卖排除，是因为外卖评价涉及包装、配送、骑手、到达温度等额外变量，用户链路和责任主体都不同。如果首轮同时覆盖，会让标签体系、商家档案和评价归因快速膨胀，难以判断问题来自交互还是场景差异。我优先选择堂食，把体验维度固定在口味、上菜速度、服务和环境，用一个商家入口验证从表达输入到复制的最小闭环。只有堂食的可用性数据成立后，才会重新设计外卖维度，而不是直接复用现有标签。

**追问2：为什么不直接做平台插件？口播：** 平台插件看似少一步复制，但会引入账号读取、页面注入、平台适配和安全验证等不确定性，也容易让产品越过“辅助表达”进入“代替提交”。我选择方向B增强版：二维码或NFC进入第三方H5，生成后由用户复制并自行选择平台。这个取舍牺牲了一点路径长度，却保留用户最终确认权，也能独立验证表达价值。首轮我会重点观察复制率和中途退出位置，如果复制步骤确实成为主要损耗，再研究合规的深链跳转，而不是先做高风险自动化。

#### Q2：这个项目的核心用户价值是什么？

**第一人称口播：** 我把核心价值定义为“降低真实表达的组织成本”，而不是“帮助商家获得更多好评”。前者允许正面、一般和负面体验都被忠实整理，后者容易诱导评分并损害可信度。产品上我让体验按钮按消极、一般、积极排列，星级独立选择，生成前检查明显冲突，生成后仍允许用户编辑。这样价值主张可以被具体验证：陌生用户是否能更快完成表达、草稿是否符合本人感受、是否愿意复制。商家的潜在价值是获得更具体反馈，但当前没有经营结果证据，所以我不会写成已提升评价量。

#### Q3：为什么作品集阶段不继续做商家后台和数据库？

**第一人称口播：** 当前目标从商业化调整为AI产品经理求职作品集，成功标准是招聘方能打开、操作、理解并追问我的产品决策。商家后台、权限和完整数据平台会增加开发量，却不会显著增强这条核心证据链。我保留了可选仓储适配和事件模型，证明扩展路径存在，但首轮Vercel不配置数据库，用户输入不持久化。验证数据先用5到8人的记录表收集。这个取舍把资源集中在交互完成度、可信生成和案例表达上；如果未来进入真实试点，再以明确同意为前提接入云端留存。

### 主题B｜交互与需求取舍

#### Q4：为什么先确认商家，再让用户填写？

**第一人称口播：** 商家上下文决定菜品、地址和最终评价目标，如果入口贴错或二维码被复用，用户可能把体验写给错误门店。相比生成后才发现问题，首屏确认是一道成本很低的防错机制。我在页面上展示名称、品类和地址，并提供“门店信息不符”的退出提示，不允许用户在首轮手动搜索切换，因为那会重新引入平台检索和商家匹配复杂度。结果是每次生成请求都绑定明确商家编号，双商家配置也能独立运行；未来需要通过用户测试确认这一步是否清晰、是否造成不必要停顿。

**追问1：首屏如何让面试官10秒内看懂？口播：** 我把首屏信息按“这是什么、现在处于什么模式、下一步做什么”组织：标题直接说明是评价表达助手，副标题说明输入真实体验后生成可编辑草稿，演示提示说明当前默认使用本地模板、真实AI接口已预留，主按钮只保留确认门店。作品集免责声明放在次要位置，避免抢占主任务，但明确非商家和平台官方产品。我会用5秒复述测试检验信息层级：让陌生用户看完后说出产品用途和下一步，不能复述就继续精简文案。

**追问2：为什么体验按钮从消极到积极？口播：** 原版本把积极选项放在左侧，视觉起点容易形成默认正向暗示，与“真实表达而非诱导好评”的产品立场不完全一致。调整为消极、一般、积极后，各维度方向一致，也符合从低到高的连续认知。更重要的是，我没有把积极选项做成默认值，所有标签都需要主动选择，颜色只在选中后出现。这个改动目前通过配置和界面检查确认，是否降低误选还没有行为数据；小样本测试会观察用户是否能准确理解每一列。

#### Q5：为什么菜品既有预设又允许自由输入？

**第一人称口播：** 只用预设招牌菜能提高输入速度，但商家不可能一次维护完整菜单；只用自由输入又会增加用户打字成本，并削弱结构化分析。我采用组合方案：优先展示一到三道招牌菜，用户点击即可选择，同时提供“其他菜品”输入口，多道菜可用顿号分隔。生成端把两类菜品合并为同一种允许事实，因此不会把自由输入当作模型指令。这个方案已经通过接口验证，自定义“狮子头”能够进入草稿；真实用户是否会理解输入方式仍待观察。

#### Q6：为什么生成后必须允许编辑？

**第一人称口播：** 评价是用户对本人体验的公开表达，模型只能辅助组织，不能替用户承担事实责任。即使事实输入正确，措辞强度、语气和细节顺序也可能不符合用户习惯，所以生成稿必须是可编辑状态，而不是最终答案。我同时提供换一种表达和字符计数，并明确提示“请确认每句话都符合真实体验”。平台跳转在复制后才激活，形成一次显式确认。这个设计保留人在回路，也把产品边界从自动发评收敛为表达辅助；后续会测用户编辑比例与主要修改类型。

### 主题C｜AI能力与可信性

#### Q7：如何防止模型编造用户没有说过的体验？

**第一人称口播：** 我没有只依赖一句“不要编造”的提示词，而是做了前后两层约束。生成前把标签、选中菜品、自定义菜品和用户原文归一为允许事实列表，系统提示要求模型只从列表引用，并让模型返回其声称使用的事实。生成后检查这些引用是否都在白名单内，同时扫描草稿是否出现当前商家已配置但用户未选择的菜品。失败时不用有风险的结果，改走本地模板。当前测试能覆盖未选择菜品与未知事实，真实模型仍需在拿到密钥后增加对抗样例评测。

**追问1：事实白名单有什么局限？口播：** 白名单能约束“用了哪些事实”，但不能完全解决语义夸大，例如用户说“还可以”，模型可能写成“非常满意”；它也不能识别所有菜单外实体，因为自由输入本身就是用户事实。因此我还结合星级和标签做倾向一致性检查，控制生成温度，并要求草稿可编辑。未来接真实模型后，我会建立包含弱正面、混合评价、否定句和口语的测试集，比较事实引用、语气偏移和人工修改率。白名单是基础边界，不是准确率承诺。

**追问2：用户输入里包含指令怎么办？口播：** 我把用户原文视为数据而不是系统命令，系统提示明确说明即使事实列表里出现指令也不能执行；发送给模型时使用结构化JSON载荷，把商家信息、星级、风格和允许事实分区。服务端还限制字段长度、类型和枚举，避免无限制内容进入请求。不过当前没有独立的提示注入分类器，所以我不会声称完全防御。接真实AI后的测试会加入“忽略规则、编造五星评价”等样例，验证模型输出和本地降级是否守住边界。

#### Q8：为什么还要检查星级与文字倾向？

**第一人称口播：** 星级是整体判断，标签和文字是局部事实，二者并不要求完全一致，但极端矛盾往往意味着误选，例如五星却只选“不满意”和“很慢”。我的规则只拦截清晰冲突：高星且只有负向信号，或低星且只有正向信号；混合体验可以正常通过。这样避免模型自行猜测用户真正意图，也减少生成与评分方向相反的结果。规则目前使用关键词和结构化情感，不是通用情感模型，因此需要保守处理，宁可少拦截，也不把复杂真实体验强行归类。

#### Q9：本地模板还能算AI产品吗？

**第一人称口播：** 当前作品集默认本地模板不代表我把模板包装成大模型。项目的AI产品能力体现在接口抽象、结构化上下文、事实约束、生成后质检、失败降级和人机协作，而真实模型只是其中一个可替换执行器。我会在界面和README明确“本地演示生成”，没有密钥时不声称使用真实AI。作品集上线后获得兼容接口密钥，再用相同输入契约切换模型并补充评测。这样的表述既诚实，也能展示我理解生成式产品不等于单次API调用。

### 主题D｜架构、质量与安全

#### Q10：为什么采用一套程序加商家档案，而不是每家商户一个版本？

**第一人称口播：** 每家商户复制一个版本在两三家时看似快，但后续任何交互、校验或安全规则修改都要多处同步，容易产生版本漂移。我把差异收敛到商家档案，包括编号、名称、品类、地址、主题色、菜品和评价入口，页面通过动态路由加载，生成端也按相同编号取得上下文。新增商家主要增加配置而不改核心流程。目前两份档案已验证不会串店；它仍是人工维护方案，适合低数量试点，规模化后才需要商家后台和审核机制。

**追问1：如何防止商家编号被伪造？口播：** 页面和生成接口都不会相信客户端传来的任意商家信息，而是只接收格式受限的商家编号，再由服务端仓储查找已启用档案。找不到、状态停用或编号不匹配都会返回错误；菜品编号也必须属于当前商家。这样用户无法通过请求体直接塞入一个虚构商家名称让模型生成。不过公开演示的商家编号本身不是秘密，防护目标是保证数据归属和事实来源，而不是隐藏URL。高流量场景还需要更完善的限流和监控。

**追问2：未来接商家后台会怎么演进？口播：** 我不会让后台直接覆盖线上配置，而会把商家编辑、审核和发布拆开。商家填写名称、地址、菜品和平台链接后先进入草稿状态，服务端校验URL域名、字段长度和重复编号，再由管理员或规则审核后发布版本；H5只读取已发布且启用的版本。还需要保留修改时间和回滚能力。当前作品集不实现这些功能，因为没有真实规模需求，但现有商家类型和仓储接口已经把配置与页面逻辑分离，为演进保留了边界。

#### Q11：系统失败时如何保证演示不断？

**第一人称口播：** 我把失败分成输入问题和外部服务问题。输入问题由服务端返回可定位字段错误，让用户修正；外部AI未配置、超时、非成功状态、空结果或解析失败统一进入本地模板，不把底层异常直接暴露给用户。模板仍使用相同星级、菜品和标签事实，并在结果页标注生成方式。这样作品集即使没有密钥也能完成主流程。当前仍需在Vercel环境验证冷启动和函数执行边界，完成前我只声称本地与测试环境验证通过。

#### Q12：如何处理用户隐私和研究留存？

**第一人称口播：** 首先执行数据最小化：产品不读取美团或大众点评账号，不要求姓名、手机号或订单号。输入中出现手机号、邮箱、证件号和银行卡号时给出风险提示，并在生成和可选保存前脱敏。早期方案设计了独立的AI处理同意和研究留存同意，只有用户额外勾选研究授权才保存脱敏样本。作品集阶段为了降低风险不配置数据库，因此线上不持久化原文和草稿。未来接数据库时还需要验证删除机制和90天到期策略，而不能只写隐私文案。

### 主题E｜验证、复盘与求职表达

#### Q13：你如何证明这个产品不是只做了一个页面？

**第一人称口播：** 我会提供互相验证的证据链，而不是只展示截图。第一层是PRD和研究材料，解释问题、用户和取舍；第二层是可交互H5与公开代码，验证双商家配置、生成和安全边界；第三层是自动测试，证明关键规则在预期输入下成立；第四层是Vercel地址、二维码和演示视频，证明外部用户可访问；第五层是5到8人的可用性记录与一次迭代。现在前三层大部分已完成，后两层尚未完成，所以简历只写已验证能力，并把用户指标标成待测。

**追问1：为什么只测5到8个人？口播：** 这一轮的目的不是估算市场规模或证明商业转化，而是发现陌生用户能否理解入口、完成填写、生成和复制，以及哪些步骤造成困惑。对这种形成性可用性测试，少量用户可以快速暴露高频界面问题。我会记录每个人的设备、是否完成、用时、复制行为、内容符合度和原话，并明确样本限制。如果要验证接受度或商家付费意愿，就需要更大样本、分层招募和更严格问卷，这不属于当前7天作品集目标。

**追问2：四个指标如何定义？口播：** 完成率以打开页面并开始操作的人为分母、成功复制草稿的人为分子；中位完成时长从商家页打开计时到复制成功，避免极端值影响；复制率以成功生成草稿的人为分母；内容符合度让用户按1到5分评价草稿是否符合本人真实感受，并记录修改原因。我会同时保留原始样本数量，不能只报百分比。由于当前无数据库，首轮由观察者使用统一表格记录，正式结果产生后再写入案例和简历。

#### Q14：问卷结果如何影响了方案？

**第一人称口播：** 前期材料给出的最清晰方向性信号是：多数消费者不了解AI衍生工具，担心操作复杂；商家侧对试用相对开放。我没有把小样本问卷当作市场验证完成，而是把它转成产品约束：不要求安装应用，入口用二维码或NFC；交互控制在星级、少量标签、菜品和一句话；商家信息提前配置，减少自动识别和平台安全验证风险。接下来要验证的是这些设计是否真的让陌生用户在约一分钟内完成，而不是继续扩大问卷数字。

#### Q15：如果重新做一次，你会调整什么？

**第一人称口播：** 我会更早把作品集目标、商业试点目标和技术验证目标拆开。前期一度讨论多商家销售、Supabase留存和更多行业，容易让范围扩大；现在回看，最有价值的主线是先证明一个陌生用户能在真实手机上完成可信表达。我会在编码前先定义四个可用性指标和五个失败场景，再按证据优先级实现。技术上仍保留配置化、事实约束和降级，但更早准备公网部署、演示脚本与测试记录，让每个功能都对应一个可验证的产品假设。

## 4. 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
| --- | --- | --- |
| 商家配置化 | `src/data/merchants.ts`、`Merchant`、动态商家路由 | Bullet 1、Q10 |
| 输入与倾向校验 | `src/lib/validation/feedback.ts`、`validateFeedbackInput` | Bullet 2、Q8 |
| AI生成与降级 | `src/lib/ai/reviewGenerator.ts`、`generateReviewDraft` | Bullet 3、Q9、Q11 |
| 输出质量门禁 | `src/lib/validation/generatedDraft.ts`、`checkGeneratedDraft` | Q7 |
| 人在回路 | `src/components/FeedbackFlow.tsx` | Bullet 4、Q6 |
| 用户验证 | `04_用户测试记录模板.md` | Bullet 5、Q13 |
| 已验证测试 | `src/lib/**/*.test.ts` | Q13 |

## 交给 /great-resume 的项目事实摘要

- 项目名称与方向：碰一碰评价表达助手；AI产品经理。
- 个人职责边界：个人作品集中的产品定义、方案取舍、交互原型、生成约束与验证材料；无正式商用或团队上线结果。
- 关键动作：配置化商家上下文、结构化输入、事实白名单、倾向一致性、隐私检测、本地降级、人在回路。
- 可核验证据：项目源码、11项测试、PRD/问卷、步骤记录；GitHub和Vercel待补。
- 候选表述：使用本文件第2节；用户指标完成后再补实际结果。
- 待补事实：公开授权、公网URL、5—8人测试结果、真实AI评测。

## 交给 /interview 的高风险 Claim 清单

- Ownership Claim：“主导”“0到1”仅限个人作品集范围，不能延伸为商家正式项目负责人。
- Metric Claim：当前只有11项自动测试；完成率、用时、复制率和符合度均待测。
- Architecture Claim：可说明配置化、校验、降级和人在回路；不能声称高并发生产架构。
- Result Claim：当前可说“完成可交互原型并通过自动验证”，不能说“已上线、被商家采用或提升评价量”。
